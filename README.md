This project uses FastAPI as a file server and API for a rich client prototype. Parcel handles the builds, use whatever JS tools that make you happy 😁

```sh
pipenv install
npm install
npm run build
pipenv run uvicorn main:app --reload
```

